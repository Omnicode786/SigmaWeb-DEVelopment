<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">    
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
        

</head>
<body>


<div class="w-full h-full bg-zinc-900 p-20">
    

    <a class="text-blue-500" href="/">Back to Home</a>

<h1 class="text-3xl tracking-tighter mb-3 text-white">All Users</h1>

    <div class="users flex gap-3 flex-wrap ">
       <% if (users.length >0){ %>
        <% users.forEach(function(user){ %>
              <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
            <div class="w-full h-72 rounded-lg bg-red-500">
                <img class="w-full h-full object-cover rounded-lg object-center" src="<%= user.image %>" alt="">
            </div>
            <h3 class="text-2xl tracking-tighter text-white"><%= user.name %></h3>
            <h5 class="text-zinc-500"> <%= user.email %></h5>
            <div class="flex gap-3 mt-10">
                <a class="text-zinc-400" href="/edit/<%=user._id%>">Edit User</a>
                <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
            </div>
        </div>
        <%  })%>
      <%   }else{ %> 
            <h3 class="text-2xl text-zinc-500 tracking-tighter text-white">no users yet</h3>
        <% }  %>
<h3></h3>

    </div>



</div>


</body>
</html>